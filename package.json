{
  "name": "agrotrace-pro",
  "version": "2.0.0",
  "description": "Sistema Professionale di Tracciabilità Agroalimentare per Produttori Artigianali",
  "main": "index.js",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "node index.js",
    "vercel-build": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
